<snippet>
	<content><![CDATA[
parser = OptionParser(usage = "%prog [options]")
parser.add_option("", "--host", dest = "host", help = "Database host.")
parser.add_option("-d", "--database", dest = "database", default = "$1",
				  help = "Database to use.")
parser.add_option("-u", "--user", dest = "user", default = "$2",
				  help = "Database user.")
parser.add_option("-p", "--password", dest = "password", default = $3,
				  help = "Database password.")
parser.add_option("", "--port", dest = "port", default = ${4:5432},
				  help = "Database port.")
$0
options, args = parser.parse_args(sys.argv[1:])
]]></content>
	<tabTrigger>OptionParser</tabTrigger>
	<scope>source.python</scope>
</snippet>
